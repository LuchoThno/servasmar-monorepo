// Nota: la lógica de correos del formulario de contacto vive en Next.js.
// Este archivo se mantiene solo como compatibilidad si el backend expone /api/contact.
// En el proyecto actual, el frontend hace fetch('/api/contact') hacia:
// apps/web/src/app/api/contact/route.ts

export default {} as any

